[Unit]
Description=Application launch booster for Qt4
After=pre-user-session.target
Requires=dbus.socket pre-user-session.target

[Service]
Type=notify
ExecStart=/usr/libexec/mapplauncherd/booster-qt4 --systemd

[Install]
WantedBy=user-session.target
