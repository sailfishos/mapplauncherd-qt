# Tell qmake to use pkg-config to get correct compiler and linker
# flags to make an application usable with applauncherd.

CONFIG += link_pkgconfig

equals(QT_MAJOR_VERSION, 4): PKGCONFIG += qdeclarative-boostable
equals(QT_MAJOR_VERSION, 5): PKGCONFIG += qdeclarative5-boostable

